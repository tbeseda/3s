extends layout

block content
  h3 Files in Your S3 Bucket
  h5
    | Bucket:
    code #{s3_response.Name}
  p Click filename for public URL
  ul#the-files.unstyled
    - for file in s3_response.Contents
      li: a(data-key=file.Key)= file.Key
